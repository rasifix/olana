<div class="grid12">
 <ul class="event-list">
 {{#each model.courses as |course|}}
  <li {{action 'courseClicked' course.id}}>
   {{#link-to 'course' course.id}}
   {{course.id}} 
   {{/link-to}}
   <div class="pull-right">
     <span class="mute">{{course.distance}}m</span>
     <span class="mute">{{course.ascent}}m</span>
     <span class="mute">{{course.controls}} Posten</span>
   	 <span class="badge">{{course.runners.length}}</span> 
   </div>
  </li>
 {{/each}}
 </ul>

</div>
