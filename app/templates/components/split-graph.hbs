  <rect x="0" y="0" width={{width}} height={{height}} fill="#f8f8f8"/>
	<rect x={{padding.left}} y={{padding.top}} width={{area.width}} height={{area.height}} fill="none" stroke="#aaa"/>

  <g id="grid">
    <path id="vgrid" d={{vgrid}} stroke="#aaa" class="grid"/>
    <g id="hgrid"></g>
  </g>

	<g id="timelines"></g>  
	
	<g id="statusline"></g>